import './Card';

const Card = () => {
    return (
        <section className="card">
            <h2 className="section-header">Digital Library Cards</h2>
        </section>
    )
};

export default Card;
